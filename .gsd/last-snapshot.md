# GSD context snapshot (2026-06-02T15:40:12.173Z)

## Top project memories
- [MEM004] (convention) The lib crate name for this project is 'amux' (from Cargo.toml [package] name). All imports from the library in main.rs and tests use `use amux::...`. This was established during M002/S01 when creating the lib/bin split.
- [MEM006] (gotcha) GSD auto-mode for non-web Rust projects: the browser evidence gate in validate-milestone.js scans all acceptance criteria text with BROWSER_REQUIREMENT_RE (matches "screenshot", "visible", "click", "snapshot", etc.). If any ASSESSMENT file is empty/missing, it forces a downgrade to needs-attention. Fix: always populate ASSESSMENT.md with "non-browser terminal application" disclaimer text. The NO_BROWSER_EVIDENCE_RE regex in browser-evidence.js matches phrases like "not a web application" and "no browser" to skip the gate.
- [MEM007] (gotcha) Patched GSD engine's validate-milestone.js to respect browser:false in PREFERENCES.md. The browserEvidenceGateRequiresAttention function now checks loadEffectiveGSDPreferences()?.preferences?.browser === false before scanning acceptance criteria. This is a local patch to the pnpm store — it will be overwritten on gsd-pi upgrade. The patch file is in browser-evidence.js and validate-milestone.js at the gsd-pi dist/resources/extensions/gsd/ path.
- [MEM005] (pattern) Rust module decomposition pattern: when splitting a large impl block into sub-modules, use `pub(super)` visibility for methods that must be called from the parent module's other sub-modules. Use `impl super::App` in child modules to extend the parent type. Child modules in Rust have access to parent's private fields, but methods defined in child impl blocks are private to that child — hence pub(super) is needed for cross-calls.
- [MEM001] (architecture) Order M002 slices by ascending risk: S01 foundation → S02 decomposition → S03 test migration Chose: Foundation first, then decomposition, then test migration. Rationale: S01 (lib.rs + lint fixes) is zero-risk 
…[truncated]

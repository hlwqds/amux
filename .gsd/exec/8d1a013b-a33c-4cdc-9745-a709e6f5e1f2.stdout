=== src/config.rs (tail -5) ===
        }
    }

    None
}

=== src/types.rs (tail -5) ===
#[derive(Deserialize)]
pub struct ClaudeMessage {
    pub role: Option<String>,
    pub content: Option<serde_json::Value>,
}

=== src/discovery.rs (tail -5) ===
            }
        }
        _ => None,
    }
}

=== src/util.rs (tail -5) ===
    disable_raw_mode()?;
    execute!(terminal.backend_mut(), LeaveAlternateScreen)?;
    terminal.show_cursor()?;
    Ok(())
}


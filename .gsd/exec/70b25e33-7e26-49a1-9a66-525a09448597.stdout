Diff in /home/huanglin/code/agent-workspace-tui/src/app.rs:1:
 use std::{
[31m-    fs, env,
(B[m[32m+    env, fs,
(B[m     io::IsTerminal,
     path::{Path, PathBuf},
 };
Diff in /home/huanglin/code/agent-workspace-tui/src/app.rs:15:
 };
 use tui_term::widget::PseudoTerminal;
 
[31m-use crate::config::{data_dir, generate_id, save_config_file, save_session_title, title_override_path};
(B[m[32m+use crate::config::{
(B[m[32m+    data_dir, generate_id, save_config_file, save_session_title, title_override_path,
(B[m[32m+};
(B[m use crate::discovery::{discover_sessions, find_session_jsonl};
 use crate::pty::{PtyHandle, PtyState};
 use crate::types::*;
Diff in /home/huanglin/code/agent-workspace-tui/src/app.rs:263:
                 // Scrollback: Page Up/Down
                 if key.code == KeyCode::PageUp {
                     if let Some(slot) = self.ptys.get(idx) {
[31m-                        slot.handle.scroll_page_up(
(B[m[31m-                            self.last_chat_area.height.saturating_sub(2) as usize,
(B[m[31m-                        );
(B[m[32m+                        slot.handle
(B[m[32m+                            .scroll_page_up(self.last_chat_area.height.saturating_sub(2) as usize);
(B[m                     }
                     return Ok(Action::Continue);
                 }
Diff in /home/huanglin/code/agent-workspace-tui/src/app.rs:1109:
             Color::DarkGray
         };
 
[31m-        let scroll_offset = self.active_pty
(B[m[32m+        let scroll_offset = self
(B[m[32m+            .active_pty
(B[m             .and_then(|idx| self.ptys.get(idx))
             .map(|s| s.handle.scrollback_offset())
             .unwrap_or(0);
Diff in /home/huanglin/code/agent-workspace-tui/src/config.rs:78:
 }
 
 pub fn title_override_path(session_id: &str) -> PathBuf {
[31m-    data_dir().join("sessions").join(format!("{session_id}.title"))
(B[m[32m+    data_dir()
(B[m[32m+        .join("sessions")
(B[m[32m+        .join(format!("{session_id}.title"))
(B[m }
 
 pub fn legacy_title_override_path(workspace_path: &Path, session_id: &str) -> PathBuf {
Diff in /home/huanglin/code/agent-workspace-tui/src/discovery.rs:232:
                         .get("message")
                         .and_then(|v| v.as_str().map(|s| s.to_string()))
                         .or_else(|| {
[31m-                            record.get("message").and_then(|v| extract_text_from_content(v.clone()))
(B[m[32m+                            record
(B[m[32m+                                .get("message")
(B[m[32m+                                .and_then(|v| extract_text_from_content(v.clone()))
(B[m                         });
                     if let Some(t) = text {
                         let truncated: String = t.chars().take(50).collect();
Diff in /home/huanglin/code/agent-workspace-tui/src/discovery.rs:254:
     }
     Some((id, title, cwd))
 }
[31m-
(B[m 
 fn discover_claude_sessions(workspaces: &[Workspace], out: &mut Vec<Session>) {
     let projects_dir = match Agent::Claude.sessions_dir() {
Diff in /home/huanglin/code/agent-workspace-tui/src/pty.rs:1:
 use std::{
[31m-    io, sync::{
(B[m[32m+    io,
(B[m[32m+    sync::{
(B[m         Arc, RwLock,
         atomic::{AtomicBool, AtomicU64, Ordering},
         mpsc::Sender,
Diff in /home/huanglin/code/agent-workspace-tui/src/pty.rs:149:
 
     pub fn scroll_page_up(&self, page_size: usize) {
         let current = self.scrollback_offset();
[31m-        self.parser.write().unwrap().screen_mut().set_scrollback(current + page_size);
(B[m[32m+        self.parser
(B[m[32m+            .write()
(B[m[32m+            .unwrap()
(B[m[32m+            .screen_mut()
(B[m[32m+            .set_scrollback(current + page_size);
(B[m     }
 
     pub fn scroll_page_down(&self, page_size: usize) {
Diff in /home/huanglin/code/agent-workspace-tui/src/pty.rs:156:
         let current = self.scrollback_offset();
[31m-        self.parser.write().unwrap().screen_mut().set_scrollback(current.saturating_sub(page_size));
(B[m[32m+        self.parser
(B[m[32m+            .write()
(B[m[32m+            .unwrap()
(B[m[32m+            .screen_mut()
(B[m[32m+            .set_scrollback(current.saturating_sub(page_size));
(B[m     }
 
     pub fn reset_scroll(&self) {
Diff in /home/huanglin/code/agent-workspace-tui/src/types.rs:43:
         }
     }
 
[31m-    pub fn build_new_cmd(&self, workspace_path: &Path, session_name: Option<&str>) -> portable_pty::CommandBuilder {
(B[m[32m+    pub fn build_new_cmd(
(B[m[32m+        &self,
(B[m[32m+        workspace_path: &Path,
(B[m[32m+        session_name: Option<&str>,
(B[m[32m+    ) -> portable_pty::CommandBuilder {
(B[m         match self {
             Agent::Claude => {
                 let mut cmd = portable_pty::CommandBuilder::new("claude");
Diff in /home/huanglin/code/agent-workspace-tui/src/types.rs:85:
         }
     }
 
[31m-    pub fn build_resume_cmd(&self, workspace_path: &Path, session_id: &str) -> portable_pty::CommandBuilder {
(B[m[32m+    pub fn build_resume_cmd(
(B[m[32m+        &self,
(B[m[32m+        workspace_path: &Path,
(B[m[32m+        session_id: &str,
(B[m[32m+    ) -> portable_pty::CommandBuilder {
(B[m         match self {
             Agent::Claude => {
                 let mut cmd = portable_pty::CommandBuilder::new("claude");
Diff in /home/huanglin/code/agent-workspace-tui/src/types.rs:128:
     pub fn sessions_dir(&self) -> Option<PathBuf> {
         match self {
             Agent::Claude => {
[31m-                let dir =
(B[m[31m-                    PathBuf::from(std::env::var("HOME").unwrap_or_default()).join(".claude/projects");
(B[m[32m+                let dir = PathBuf::from(std::env::var("HOME").unwrap_or_default())
(B[m[32m+                    .join(".claude/projects");
(B[m                 if dir.exists() { Some(dir) } else { None }
             }
             Agent::Codex => {
Diff in /home/huanglin/code/agent-workspace-tui/src/types.rs:136:
[31m-                let dir =
(B[m[31m-                    PathBuf::from(std::env::var("HOME").unwrap_or_default()).join(".codex/sessions");
(B[m[32m+                let dir = PathBuf::from(std::env::var("HOME").unwrap_or_default())
(B[m[32m+                    .join(".codex/sessions");
(B[m                 if dir.exists() { Some(dir) } else { None }
             }
             Agent::Gsd => {
Diff in /home/huanglin/code/agent-workspace-tui/src/main.rs:265:
 
     #[test]
     fn parse_gsd_session_no_session_line() {
[31m-        let jsonl = r#"{"type":"custom_message","customType":"gsd-run","message":"no session header"}"#;
(B[m[32m+        let jsonl =
(B[m[32m+            r#"{"type":"custom_message","customType":"gsd-run","message":"no session header"}"#;
(B[m         let dir = std::env::temp_dir().join("agent-test-gsd-no-session");
         let _ = std::fs::create_dir_all(&dir);
         let path = dir.join("bad.jsonl");
Diff in /home/huanglin/code/agent-workspace-tui/src/main.rs:405:
         };
 
         // When PTY is alive, all agents are retained
[31m-        assert!(should_retain(Agent::Claude, true), "Claude should retain when alive");
(B[m[31m-        assert!(should_retain(Agent::Codex, true), "Codex should retain when alive");
(B[m[31m-        assert!(should_retain(Agent::Gsd, true), "GSD should retain when alive");
(B[m[32m+        assert!(
(B[m[32m+            should_retain(Agent::Claude, true),
(B[m[32m+            "Claude should retain when alive"
(B[m[32m+        );
(B[m[32m+        assert!(
(B[m[32m+            should_retain(Agent::Codex, true),
(B[m[32m+            "Codex should retain when alive"
(B[m[32m+        );
(B[m[32m+        assert!(
(B[m[32m+            should_retain(Agent::Gsd, true),
(B[m[32m+            "GSD should retain when alive"
(B[m[32m+        );
(B[m 
         // When PTY exits, only Codex is removed — GSD and Claude persist
[31m-        assert!(should_retain(Agent::Claude, false), "Claude sessions MUST persist after PTY exit");
(B[m[31m-        assert!(!should_retain(Agent::Codex, false), "Codex sessions should be cleaned up after PTY exit");
(B[m[31m-        assert!(should_retain(Agent::Gsd, false), "GSD sessions MUST persist after PTY exit");
(B[m[32m+        assert!(
(B[m[32m+            should_retain(Agent::Claude, false),
(B[m[32m+            "Claude sessions MUST persist after PTY exit"
(B[m[32m+        );
(B[m[32m+        assert!(
(B[m[32m+            !should_retain(Agent::Codex, false),
(B[m[32m+            "Codex sessions should be cleaned up after PTY exit"
(B[m[32m+        );
(B[m[32m+        assert!(
(B[m[32m+            should_retain(Agent::Gsd, false),
(B[m[32m+            "GSD sessions MUST persist after PTY exit"
(B[m[32m+        );
(B[m     }
 
     #[test]
